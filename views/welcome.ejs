<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Status</title>
</head>
<body>
    <% if (!user) { %>
        <!-- Render login form -->
        <%- include('login.ejs') %>
    <% } else { %>
        <!-- Render dashboard -->
        <%- include('home.ejs') %>
    <% } %>
</body>
</html>
